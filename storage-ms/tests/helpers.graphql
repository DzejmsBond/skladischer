query FilterItems($username: String!, $storage_name: String!, $filtering: Filter!) {
    items(username: $username, storage_name: $storage_name, filtering: $filtering) {
        code_id
        description
        amount
    }
}